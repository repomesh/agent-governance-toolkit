# agent-control-specification-opa-darwin-x64

Vendored Open Policy Agent binary used by `agent-control-specification`.
The binary is populated by `scripts/fetch-opa.mjs` at publish time and is not committed.
